{
  "name": "bloglist",
  "version": "1.0.0",
  "description": "Allows users to save information about blogs they know of.",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "test": "NODE_ENV=test jest --verbose --runInBand --forceExit",
    "testuserapi": "NODE_ENV=test jest --verbose --runInBand --forceExit user_api.test.js",
    "testblogapi": "NODE_ENV=test jest --verbose --runInBand --forceExit blog_api.test.js",
    "start:test": "NODE_ENV=test node index.js"
  },
  "author": "Damian Davila",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.5.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "nodemon": "^2.0.19",
    "supertest": "^6.2.4"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
